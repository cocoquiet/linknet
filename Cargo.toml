[package]
name = "linknet"
version = "0.1.0"
edition = "2024"

[dependencies]
regex = "1.0"
reqwest = { version = "0.13.4", features = ["blocking"] }
scraper = "0.27.0"
eframe = {version = "0.35.0", features = ["persistence"] }
egui = "0.35.0"